import React from "react";
import { MainTemplate } from "./components";

/* this will be a page for the template chosen by the user to create his resume where he will be editing and adding his detail on a particular resume */

const page = () => {
  return <MainTemplate />;
};

export default page;
